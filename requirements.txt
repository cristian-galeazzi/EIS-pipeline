# EIS Pipeline - Python dependencies
# Install with: pip install -r requirements.txt
#
# Versions are PINNED to the environment that produced the validated results.
# Numeric output (DRT, Zarc fit) is only byte-reproducible against these exact
# versions - every export also records them in its Metadata sheet.
# (Wilson et al., "Good enough practices in scientific computing", 2017.)

# Core numerics
numpy==2.4.2
pandas==3.0.1
scipy==1.17.1
matplotlib==3.10.8

# EIS / file import
zahner_analysis==1.1.5    # read Zahner .ism binary files

# EIS analysis
pyDRTtools==0.2           # BHT quality test + DRT (Tikhonov / mGCV)
cvxopt==1.3.2             # required by pyDRTtools (not auto-installed)
scikit-learn==1.8.0       # required by pyDRTtools (not auto-installed)
impedance==1.7.1          # evaluates the fitted Zarc circuit for the plot overlays

# Export
openpyxl==3.1.5           # .xlsx read/write

# Optional (UX only - pipeline runs without it)
ipykernel>=6.0            # required by VS Code / Jupyter to run notebooks
ipywidgets>=8.0           # live control panels in NB02 / NB03 / NB04
tqdm>=4.0                 # progress bars in NB02 batch KK loop

# Development / testing
pytest>=8.0               # run with: pytest tests/
ruff==0.15.17             # lint: ruff check pipeline tests tools audit (also run in CI)
nbstripout>=0.7           # strips notebook outputs on git commit (run: nbstripout --install)

# stdlib (no pip install needed): pathlib, shutil, datetime, re
